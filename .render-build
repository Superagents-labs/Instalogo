#!/bin/bash
# Force clean dependency installation for Render deployment
echo "🧹 Cleaning dependency cache..."
rm -rf node_modules package-lock.json
echo "📦 Installing dependencies with new @types packages..."
npm install --force
echo "🔨 Building TypeScript..."
npm run build
echo "✅ Build completed successfully!"
